# Disabled — production is Netlify (getstint.de). Kept for reference.
name: Deploy to GitHub Pages

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages deploy is disabled; use Netlify."
