{
  "name": "stint",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:blog": "npm run dev --prefix blog",
    "postinstall": "npm install --prefix blog",
    "build": "tsc && vite build && npm run build --prefix blog",
    "build:blog": "npm run build --prefix blog",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fontsource/fraunces": "^5.2.5",
    "@fontsource/source-sans-3": "^5.2.6"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^0.21.1"
  }
}
